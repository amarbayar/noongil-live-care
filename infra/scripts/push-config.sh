#!/bin/bash
set -e

echo "==> Pushing config to Firestore / Remote Config..."
echo "TODO: Implement config push using firebase-tools CLI"
echo "  - feature-flags.json -> Firebase Remote Config"
echo "  - theme-default.json -> Firestore /config/theme"
echo "  - strings/*.json -> Firestore /config/strings/{locale}"
echo "  - prompts/*.txt -> Firestore /config/prompts/{name}"
