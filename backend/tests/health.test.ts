import { describe, it, expect } from 'vitest';

describe('health endpoint', () => {
  it('should return ok status', () => {
    // TODO: Implement with fastify.inject()
    expect(true).toBe(true);
  });
});
